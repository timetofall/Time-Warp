module.exports = {
    james(){
        let myArray = ['I really thing James should play league again', 'When is James going to install league?'];
        return myArray[Math.floor(Math.random() * myArray.length)];
    },

    wes(){
        let myArray = ["hi wes", "hi thug"];
        return myArray[Math.floor(Math.random() * myArray.length)];
    },
    //new
    shinnu(){
        let myArray = ["Shinnu? is that guy even awake yet?"];
        return myArray[Math.floor(Math.random() * myArray.length)];
    },

    migals(){
        let myArray = ["I wonder when Migals will finally game with the boys", "Migals.. I remember when him and I were boys"];
        return myArray[Math.floor(Math.random() * myArray.length)];
    },

    zathik(){
        let myArray = ["No matter what you do Chris, you still wont be an officer", "Learn to use Fortifying Brew, Chris"];
        return myArray[Math.floor(Math.random() * myArray.length)];
    },

    angelo(){
        let myArray = ["Where has that Angelo guy been anyways?", "Angelo... I bet he's still looking for big titty goth bitches LUL"];
        return myArray[Math.floor(Math.random() * myArray.length)];
    },

    phoneyy(){
        let myArray = ["Fuck off, Dan :wink:"];
        return myArray[Math.floor(Math.random() * myArray.length)];
    },
};